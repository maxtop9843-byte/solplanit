# 솔플래닛 리디자인 실행 지시서

**저장소:** https://github.com/maxtop9843-byte/solplanit
**대상:** https://www.solplanit.com/
**스택:** Next.js · Tailwind · Vercel

---

## 0. 최우선 규칙 — 먼저 읽어라

### 무시할 것

이 저장소와 이전 대화에 남아 있는 **모든 기존 디자인 문서와 원칙을 무시한다.**

- 저장소의 기존 `DESIGN.md` — 폐기. 새 `DESIGN.md`로 완전히 대체한다
- `STYLE.md`, `BRAND.md`, `design-tokens.*`, 이전 `CLAUDE.md`의 디자인 관련 지시 — 전부 폐기
- 기존 코드에 박혀 있는 색상값·폰트·간격 — 참조하지 않는다
- 이전 히어로 영상 기획, 하늘→패널 전환 연출, 시공사진 활용 계획 — 전부 폐기

기존 값을 하나라도 살려서 새 토큰과 섞으면 실패다. **토큰은 새 `DESIGN.md`가 유일한 출처다.**

### 유일한 진실

**저장소 루트의 새 `DESIGN.md`.** 색·폰트·간격·구조에 대한 모든 판단은 이 문서에서만 가져온다. 문서에 없는 값이 필요하면 만들지 말고 사용자에게 묻는다.

### 목표

레퍼런스 사이트와 동급이 아니라 **그 이상**의 완성도. 다만 "이상"의 정의는 장식을 더하는 게 아니라 **더 적은 요소로 더 정확하게 만드는 것**이다.

---

## 1. 사용할 스킬 — 단계별로 지정된 것만

각 단계에서 지정된 스킬을 실제로 로드하고 그 지침을 따른다. 이름만 언급하고 넘어가지 않는다.

| 단계 | 스킬 |
|---|---|
| 1. 레퍼런스 분석 | `frontend-design`, `design-taste-frontend` |
| 2. 감사 | `ponytail:ponytail-audit`, `redesign-existing-projects` |
| 3. 토큰 구현 | `ui-ux-pro-max:design-system`, `ui-ux-pro-max:ui-styling` |
| 4. 화면 구축 | `ui-ux-pro-max:ui-ux-pro-max`, `ux-designer`, `minimalist-ui` |
| 5. 한국어 카피 | `design:ux-copy` → `korean-skills:grammar-checker` → `korean-skills:humanizer` → `korean-skills:style-guide` |
| 6. 검수 | `design:accessibility-review`, `ponytail:ponytail-review`, `design:design-critique` |
| 전 구간 | `ponytail:ponytail` (lite), `andrej-karpathy-skills:karpathy-guidelines` — 적용 범위는 아래 두 소절에 명시. 반드시 읽고 시작할 것 |

### 포니테일 적용 범위 — 반드시 지켜라

`ponytail:ponytail`은 **lite 강도**로, **구현에만** 적용한다.

**적용한다:**
- 새 의존성 추가 금지. 애니메이션 라이브러리, 아이콘 패키지, UI 킷 전부 불필요하다
- 새 추상화 레이어 금지. 컴포넌트 팩토리, 테마 프로바이더, 유틸 래퍼 만들지 않는다
- 새 파일을 만들기 전에 기존 파일 수정으로 되는지 먼저 확인한다
- 지우는 것이 고치는 것보다 우선이다
- 파일 전체 재생성 대신 부분 수정을 쓴다
- 전체 스캔 대신 필요한 파일만 읽는다. 대용량 파일은 범위를 지정해 읽는다
- 중간 요약·진행 보고를 길게 쓰지 않는다. 작업하고 결과만 말한다

**적용하지 않는다:**
- 디자인 충실도. `DESIGN.md`의 타이포 스케일, 간격, 한글 보정값을 "과하다"는 이유로 줄이지 않는다
- 접근성. 대비·포커스·터치 타깃은 협상 대상이 아니다
- 한국어 문장 품질
- **작업의 존재 이유.** 포니테일의 첫 원칙은 "이 작업이 필요한가"를 묻는 것이지만, **리디자인의 필요성 자체는 논쟁 대상이 아니다.** 이미 결정된 사항이다. "사이트가 이미 동작한다"거나 "지금도 충분히 깔끔하다"는 이유로 범위를 줄이지 않는다

`DESIGN.md`의 값을 줄이는 것이 더 낫다고 판단되면, **적용하지 말고 근거와 함께 보고한다.** 조용히 줄이는 것이 이 작업에서 가장 흔한 실패 방식이다.

포니테일로 디자인을 깎으면 이 작업의 목적이 사라진다. 포니테일은 **코드를 줄이는 도구지 디자인을 줄이는 도구가 아니다.**

### 카파시 가이드라인 — 작업 방식

`andrej-karpathy-skills:karpathy-guidelines`를 전 구간에 적용한다. **설치돼 있지 않다면 아래 규칙을 그대로 지킨다.**

포니테일과 역할이 다르다. **포니테일은 "무엇을 만들지 말지"(범위)를, 카파시는 "어떻게 고칠지"(방식)를 정한다.** 충돌하면 범위는 포니테일, 방식은 카파시를 따른다.

**1. 수술적으로 고친다.** 요청받은 것만 바꾼다. 지나가다 발견한 개선점은 고치지 말고 목록으로 보고한다. 리팩터링과 기능 변경을 한 커밋에 섞지 않는다. 잘 돌아가는 코드를 취향으로 다시 쓰지 않는다.

**2. 가정을 먼저 드러낸다.** 코드를 쓰기 전에 무엇을 가정하고 있는지 한 줄로 밝힌다. 확신이 없으면 추측해서 진행하지 말고 묻는다. 특히 `DESIGN.md`에 없는 값이 필요할 때, 기존 계산 로직의 동작이 불분명할 때, 삭제 대상이 다른 곳에서 참조될 때.

**3. 성공 기준을 먼저 정한다.** 각 단계를 시작하기 전에 "무엇이 참이면 이 단계가 끝난 것인가"를 검증 가능한 형태로 적는다. 7절 완료 조건이 그 형식이다. "예뻐 보인다"는 기준이 아니다. "`box-shadow` 사용처 0건"이 기준이다.

**4. 검증하고 나서 보고한다.** 빌드를 돌리고, 실제로 렌더링을 확인하고, 완료 조건을 눈으로 대조한 뒤에 완료라고 말한다. 안 돌려본 것을 됐다고 말하지 않는다. 확인 못 한 항목은 확인 못 했다고 말한다.

**5. 모르는 것을 지어내지 않는다.** 기존 코드의 의도를 추측해서 서술하지 않는다. 파일을 읽지 않고 내용을 단정하지 않는다. 레퍼런스 사이트를 실제로 열지 않고 그 내용을 요약하지 않는다.

---

## 2. 1단계 — 레퍼런스를 실제로 방문해서 분석

추정하지 말고 **WebFetch로 직접 읽어라.**

```
https://styles.refero.design/style/0f7da1b2-9d06-4ef5-b5a8-ef7f92e57ab2   (Augen Pro)
https://styles.refero.design/style/75fdb89f-ca64-41b3-af36-7a78bd09448e   (Steep)
```

각 페이지 하단에 DESIGN.md 전문이 코드 블록으로 들어 있다. 전문을 읽고 다음을 뽑아낸다.

**Augen Pro에서 확인할 것 — 이 리디자인의 뼈대**
- 350 이하 단일 웨이트 체계와 "위계는 크기로만" 규칙이 실제로 어떻게 서술되는지
- 그림자 없이 흰색 톤 차이(#fdfdfd on #f2f2f4)와 0.5px 헤어라인만으로 층을 만드는 방식
- 액센트 색을 면으로 채우지 않는 배급 원칙
- 텍스트가 콘텐츠의 95%를 차지하는 구성

**Steep에서 확인할 것 — 숫자 표현만**
- 스탯 카드가 축도 격자선도 없이 큰 수치 + 변화 한 줄로 끝나는 구조
- 액센트 면을 페이지당 한 번만 쓰는 규율

**적용할 때의 필수 치환** — 그대로 베끼면 실패한다.

| 레퍼런스 | 우리 적용 |
|---|---|
| Augen 히어로의 3D 렌더가 시각적 무게를 감당 | **그 자리에 계산기 입력 카드를 놓는다.** 히어로 = 도구 |
| Augen 카드 라운드 54px | 16px |
| Augen 액센트 `#0071E3` | 폐기. 캔버스 위 4.20:1로 AA 미달 |
| Steep 90px 세리프 디스플레이 | 폐기. 한글 세리프는 큰 크기에서 획 대비가 심해 무거워진다 |
| Steep peach 계열 | 폐기. 태양광 클리셰 |
| PP Neue Montreal / Signifier | Pretendard Variable + Geist Mono |

분석 결과를 3~5줄로 요약해서 보고한 뒤 다음 단계로 간다. 길게 쓰지 않는다.

---

## 3. 2단계 — 감사와 삭제

`ponytail:ponytail-audit`로 저장소를 훑고, 아래를 **먼저 지운다.** 새로 만들기 전에 지우는 게 순서다.

### 홈에서 제거

- [ ] 히어로 영상(`hero-sky.mp4`)과 배경 사진(`hero-rooftop.jpg`) — 관련 컴포넌트·에셋·CSS 전부
- [ ] 신뢰 지표 4칸 (3건 / 4단계 / PVGIS 5.3 / 전 항목)
- [ ] 시공 사례 섹션 — **Unsplash 스톡 사진을 실제 시공 현장으로 표기하고 있다. 최우선 제거**
- [ ] 광고 블록
- [ ] 진행 과정 4단계 섹션
- [ ] 커뮤니티 유도 섹션
- [ ] 중복 SEO 섹션("계산 방법과 해석")과 홈에 잘못 렌더링된 브레드크럼(`홈 > 태양광 설치 계산`)
- [ ] 무료 견적 CTA 전부 (상단바·히어로·하단) 및 `/quote` 진입 경로

### 사이트 전역

- [ ] 내비게이션에서 `/cases`, `/community` 링크 제거 — 내용이 채워져 있지 않다
- [ ] 연락처·주소를 수집하는 폼 전부 제거. **운영자는 사업자등록이 없어 개인정보를 수집할 수 없다**
- [ ] 사용하지 않게 된 이미지 에셋, 컴포넌트, 의존성 정리

### 남길 것

- [ ] 계산 방법론 / 데이터 출처 / 가정과 한계 페이지 — **이 사이트의 유일한 차별점이다. 지우지 말 것**
- [ ] `본문으로 건너뛰기` 스킵 링크
- [ ] 기존 계산 로직 (PVGIS 5.3 기반) — 로직은 유지하고 UI만 교체한다

삭제 목록을 커밋 하나로 처리한 뒤 다음 단계로 간다.

---

## 4. 3단계 — 토큰 구현

`ui-ux-pro-max:design-system`과 `ui-ux-pro-max:ui-styling`을 로드한다.

1. 새 `DESIGN.md`를 저장소 루트에 배치
2. 10절 CSS 변수 블록을 전역 스타일에 그대로 반영
3. 11절 Tailwind v4 `@theme` 블록 반영
4. **기존 하드코딩된 색·폰트·간격을 전부 토큰 참조로 치환.** 남은 하드코딩 값이 0이어야 한다
5. 폰트 로드 — Pretendard Variable, Geist Mono. `next/font`로 self-host, `display: swap`

### 한글 전역 규칙 — 빠뜨리면 즉시 망가진다

```css
html {
  word-break: keep-all;
  overflow-wrap: break-word;
  -webkit-font-smoothing: antialiased;
}
.num { font-family: var(--font-num); font-variant-numeric: tabular-nums; }
```

`word-break: keep-all`이 없으면 한글이 단어 중간에서 잘린다. 이것 하나로 전체 인상이 무너진다.

---

## 5. 4단계 — 홈 재구축

`DESIGN.md` 7절의 ASCII 구조를 그대로 구현한다. **블록은 두 개다. 늘리지 않는다.**

```
상단바 → 블록1(도구) → 블록2(근거) → 푸터
```

### 계산기 — 이번 작업의 핵심

기존 4단계 위저드(건물유형 → 면적 → 지역 → 목적)를 **입력 카드 하나로 접는다.** 주소 / 지붕 면적 / 건물 유형이 한 화면에 있고, 버튼 하나로 결과가 같은 화면에 나온다.

**범위 제한:** 지도 기반 지붕 면적 자동 산출은 이번 작업에 포함하지 않는다. 면적은 사용자 직접 입력을 유지한다. 이건 별도 작업이다. 임의로 착수하지 말 것.

### 결과 표시

- 중간값 3줄(설치 가능 용량 / 연간 예상 발전량 / 지역 보조금)은 **전부 무채색**, 행 사이 0.5px 헤어라인
- 최종값(내 부담금) 하나만 `--result-fill` 면 위에 48px Geist Mono
- **`--result-fill`이 나타나는 곳은 페이지 전체에서 이 한 곳뿐이다.** 색으로 강조하는 게 아니라 다른 데 색이 없어서 강조된다
- 숫자 카운트업 애니메이션 금지. 없는 정밀함을 연출하는 것이라 이 사이트 취지에 반한다

### 시각적 완성도의 기준

장식을 더해서 올리지 않는다. 다음으로 올린다.

- 광학 정렬 — 숫자 우측 정렬, 라벨 베이스라인 일치
- 0.5px 헤어라인의 실제 렌더링 (레티나·비레티나 모두 확인)
- 여백의 리듬 — 96px 섹션 간격을 아끼지 않는다
- 포커스 상태의 완성도 — 키보드 탐색이 마우스만큼 깔끔해야 한다
- 결과가 나타나는 200ms 전환 하나. 그 외 모션 없음

---

## 6. 5단계 — 한국어 카피

이 순서대로 실행한다. 순서를 바꾸지 않는다.

1. **`design:ux-copy`** — 라벨·버튼·오류 메시지·빈 상태 작성. 버튼 라벨은 동작이 아니라 결과를 말한다("제출" ✗ / "계산하기" ○)
2. **`korean-skills:grammar-checker`** — 맞춤법·띄어쓰기·조사
3. **`korean-skills:humanizer`** — 번역투와 AI 문체 제거. 특히 `~에 대해`, `~를 통해`, `~되어진다`, 쉼표 과다, 불필요한 복수형
4. **`korean-skills:style-guide`** — 어조 통일(경어체), 용어 통일, 숫자·단위 표기 통일

### 카피 톤 — 지켜야 할 것

기존 사이트의 카피 목소리는 이 시장에서 드물게 좋다. 리디자인하면서 매끈한 마케팅 톤으로 덮지 마라.

> "맞지 않으면 맞지 않다고 말씀드려요"
> "무리한 용량을 권하지 않는 것이 첫 번째 원칙입니다"

이 결을 유지한다. 과장·감탄사·이모지 금지.

### 반드시 들어갈 문구

중립성 배너(푸터 위)와 푸터.

```
솔플래닛은 시공사가 아닙니다.
어떤 업체로부터도 수수료나 광고비를 받지 않습니다.
계산 결과를 팔지 않고, 연락처도 받지 않습니다.
```

푸터에는 운영 주체, 이메일, 데이터 최종 갱신일, 그리고 개인이 운영하는 정보 사이트라는 표기를 넣는다. 사업자등록번호는 없으므로 넣지 않는다.

---

## 7. 6단계 — 검수

### `design:accessibility-review`

WCAG 2.1 AA 기준으로 감사한다. `DESIGN.md` 2절의 실측 대비표를 기준값으로 쓴다.

### `ponytail:ponytail-review`

과잉 구현을 찾아 지운다. 새로 추가된 추상화, 쓰이지 않는 변형, 투기적 유연성.

### `design:design-critique`

레퍼런스와 나란히 놓고 평가한다. 기준은 "예쁜가"가 아니라 **"숫자를 믿게 되는가"**다.

### 완료 조건

전부 통과해야 완료다.

- [ ] `box-shadow` 사용처 **0건**
- [ ] `font-weight` 500 이상 사용처 **0건**
- [ ] 초록·노랑 계열 색상 **0건**
- [ ] `DESIGN.md`에 없는 색상값 **0건**
- [ ] `<img>` / `next/image` 사용처 **0건** (로고 SVG 제외)
- [ ] 이모지 아이콘 **0건**
- [ ] `--result-fill` 사용처가 페이지당 정확히 **1곳**
- [ ] 견적·연락처 수집 폼 **0건**
- [ ] `word-break: keep-all` 전역 적용, 단어 중간 줄바꿈 **0건**
- [ ] 모든 숫자에 `tabular-nums` 적용
- [ ] 키보드 탐색 시 모든 인터랙티브 요소에 포커스 링이 보임
- [ ] 터치 타깃 최소 48px
- [ ] `prefers-reduced-motion` 존중
- [ ] 375 / 768 / 1024 / 1440에서 가로 스크롤 없음
- [ ] 새로 추가된 의존성 **0개**
- [ ] Lighthouse 접근성 95점 이상
- [ ] 빌드 통과, 콘솔 에러 0건

### 유지 확인 — 위 목록만큼 중요하다

위 조건들은 전부 "더하지 마라"를 검증한다. 아래는 **"빼지 마라"를 검증한다.** 무언가를 줄여서 위 목록을 통과했다면 실패다.

- [ ] 섹션 간격이 데스크톱 96px / 모바일 56px로 살아 있다
- [ ] 모든 경계선이 0.5px다. 1px로 바뀐 곳 **0건**
- [ ] Geist Mono가 제거되지 않았고 모든 숫자에 적용돼 있다
- [ ] 타입 스케일 5단계의 행간·자간이 **각각 다른 값**으로 남아 있다. 하나로 통일된 곳 0건
- [ ] 본문 웨이트 400, 제목 300~350. 제목을 400 이상으로 올린 곳 0건
- [ ] `--result-fill` 카드가 실제로 존재하고, 그 안의 48px 숫자가 **페이지에서 가장 큰 글자**다
- [ ] 도구·본문 폭 640px, 카드 안쪽 여백 32px(모바일 24px)가 유지된다
- [ ] `word-break: keep-all`과 `tabular-nums`가 제거되지 않았다

---

## 8. 진행 상태 — 세션이 끊겨도 이어갈 수 있게

이 작업은 여러 세션에 걸쳐 진행된다. 다음 세션의 너는 **이번 세션에서 무엇을 했는지 전혀 모른다.** 따라서 진행 상태를 저장소에 파일로 남긴다.

### 시작할 때

저장소 루트의 `REDESIGN-PROGRESS.md`를 **가장 먼저 읽는다.**

- 파일이 없으면 → 2절(레퍼런스 분석)부터 처음 시작
- 파일이 있으면 → 마지막 `[완료]` 다음 단계부터 시작. 이미 끝난 단계를 다시 하지 않는다
- 마지막 항목이 `[진행중]`이면 → 그 단계의 실제 코드 상태를 먼저 확인하고, 어디까지 됐는지 보고한 뒤 이어간다

### 각 단계가 끝날 때

`REDESIGN-PROGRESS.md`를 갱신하고 **같은 커밋에 포함시킨다.** 형식은 아래를 그대로 쓴다.

```markdown
# 리디자인 진행 상태

## 2. 레퍼런스 분석          [완료] 2026-08-08
- Augen Pro / Steep DESIGN.md 전문 확인
- 치환 결정: 히어로 자리에 계산기 카드, 라운드 16px, 액센트 폐기

## 3. 감사와 삭제            [완료] 2026-08-08
- 삭제: 히어로 영상·사진, 시공사례(스톡), 광고, 진행과정, 커뮤니티 유도, 중복 SEO, 견적 CTA
- 커밋: a1b2c3d
- 미해결: (없음 / 있으면 구체적으로)

## 4. 토큰 구현              [진행중]
- 완료: CSS 변수, Tailwind @theme
- 남음: 하드코딩 값 치환 (src/components/ 3개 파일)

## 5. 홈 재구축              [대기]
## 6. 한국어 카피            [대기]
## 7. 검수                   [대기]

---
## 사용자 확인이 필요한 사항
- (판단이 갈려 멈춘 지점을 여기 적는다. 없으면 "없음")
```

### 규칙

- 단계를 **완료했을 때만** `[완료]`로 표시한다. 부분적으로 했으면 `[진행중]`으로 두고 남은 일을 구체적으로 적는다
- "미해결"과 "사용자 확인이 필요한 사항"을 비워두지 않는다. 다음 세션의 너에게 가장 중요한 정보다
- 한도에 걸려 갑자기 끊길 수 있으므로, **단계 중간에도 의미 있는 진전이 생기면 갱신하고 커밋한다**

---

## 9. 작업 방식

- **커밋을 단계별로 나눈다.** `chore: 삭제` → `feat: 토큰` → `feat: 홈 재구축` → `chore: 카피` → `fix: 검수`
- 각 단계 끝에 짧게 보고한다. 서너 줄이면 충분하다
- 판단이 갈리는 지점에서는 진행하지 말고 묻는다. 특히 `DESIGN.md`에 없는 값이 필요할 때
- 절대 하지 않을 것: 임의 기능 추가, 새 페이지 생성, 지도 기능 착수, 의존성 추가, 커뮤니티 되살리기

---

## 10. 한 줄 요약

> 기존 문서와 값은 전부 버리고, 새 `DESIGN.md`만을 유일한 출처로 삼아, 레퍼런스 두 곳을 직접 읽고 그 규율을 이 사이트에 맞게 치환해서, 사진도 영상도 광고도 견적도 없는 도구 하나짜리 홈을 만든다. 페이지에서 가장 큰 글자는 헤드라인이 아니라 계산 결과 숫자다.
