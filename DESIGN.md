# DESIGN.md — SolPlanit Renewal V2

> SolPlanit은 계산기 한 장이 아니라, 태양광 설치 판단을 빠른 계산에서 정밀 분석과 근거 확인까지 이어주는 한국형 Solar Decision OS다.

이 문서는 이전 DESIGN.md를 완전히 대체한다. 이후 UI 추가·수정은 이 문서를 최우선 디자인 기준으로 삼는다.

---

## 0. 제품 전제

SolPlanit은 시공사도 견적 중개 플랫폼도 아니다. 주택·소규모 태양광을 검토하는 사람이 스스로 판단할 수 있도록 계산 도구와 근거를 무료로 제공하는 정보 제품이다.

현재 단계의 핵심 제약은 다음과 같다.

1. 견적 유도, 전화번호 수집, 영업 연락 CTA를 넣지 않는다.
2. 계정이 없어도 핵심 기능을 끝까지 쓸 수 있어야 한다.
3. 실제 보유하지 않은 시공 사진을 스톡·AI 이미지로 채우지 않는다.
4. 계산 근거가 없는 단가·보조금·발전량을 그럴듯하게 자동 입력하지 않는다.
5. 숫자에는 출처, 갱신 시점, 가정 또는 한계가 따라야 한다.

**북극성:** 사용자가 “태양광을 해야 하나?”를 감이 아니라 숫자로 판단하게 만든다.

---

## 1. 사이트 구조

SolPlanit의 기본 사용자 흐름은 네 단계다.

1. **빠른 계산** `/`
   - 건물 유형 + 지붕 면적
   - 설치 가능 용량과 예상 패널 수
   - 사전 검토용으로 최대한 짧게
2. **정밀 분석** `/pro`
   - 위치, 경사, 방위, 손실률 등
   - PVGIS 5.3 등 실제 외부 데이터 사용
3. **판단 가이드** `/guides`
   - 계산 결과를 실제 검토로 옮길 때 필요한 조건 설명
4. **방법론** `/trust/methodology`, `/trust/assumptions`
   - 공식, 가정, 출처, 한계 공개

홈은 이 네 단계를 한 제품으로 이해시키는 허브다. 계산기가 주인공인 원칙은 유지하지만, 더 이상 640px 계산기 하나만 덩그러니 놓지 않는다.

---

## 2. 홈 화면

홈은 큰 섹션 두 개를 유지한다.

### Block 1. Decision Workspace

데스크톱에서 1120px 폭의 2열 구조다.

- 왼쪽: 제품 메시지 + 01~04 분석 흐름
- 오른쪽: 실제 빠른 계산기
- 계산기는 첫 화면에서 즉시 입력 가능해야 한다.
- 별도 히어로 이미지나 영상은 넣지 않는다.
- 시각적 주인공은 문구가 아니라 실제 인터랙티브 도구다.

권장 메시지:

- Eyebrow: `SOLAR DECISION TOOLS / KOREA`
- H1: `태양광 설치, 감이 아니라 숫자로 결정하세요.`
- Trust line: `계정 없음 · 영업 연락 없음 · 계산 근거 공개`

### Block 2. Evidence

왼쪽은 “왜 믿을 수 있는가”, 오른쪽은 출처와 한계를 행 단위로 보여준다.

- 설치 용량 근거
- 발전량 입력 방식
- 정밀 분석 데이터 출처
- 갱신일
- 현장 검토가 필요한 한계

페이지 끝에서 광고성 CTA를 만들지 않는다. 사용자의 다음 행동은 `/pro`, `/guides`, `/trust` 중 하나여야 한다.

---

## 3. 시각 언어

목표는 “친환경 홈페이지”가 아니라 “정밀한 기술 제품”이다.

### 사용한다

- 차가운 오프화이트 캔버스
- 거의 검정에 가까운 잉크
- 얇은 헤어라인
- 넓은 여백
- 숫자 전용 모노스페이스
- 결과 하나에만 쓰는 옅은 패널 블루
- 실제 폼, 표, 계산 결과, 데이터 흐름 자체를 시각 요소로 사용

### 사용하지 않는다

- 태양광 업계에서 흔한 초록·노랑 메인 컬러
- 그라디언트 배경
- 카드 그림자
- 장식용 일러스트
- 스톡 태양광 사진
- AI 생성 패널·주택 이미지
- 이모지를 기능 아이콘처럼 사용
- 과한 스크롤 애니메이션
- 숫자 카운트업

---

## 4. 색

기본 토큰은 아래 8개만 사용한다.

| 토큰 | 값 | 역할 |
|---|---|---|
| `--canvas` | `#F2F2F4` | 전체 배경 |
| `--surface` | `#FDFDFD` | 폼과 카드 |
| `--ink` | `#0F1012` | 본문·주 동작 |
| `--ink-muted` | `#5E5E5E` | 보조 정보 |
| `--hairline` | `#D8D8DC` | 경계 |
| `--result-fill` | `#E4EDF7` | 핵심 계산 결과 면 |
| `--result-ink` | `#123A5E` | 결과 숫자 |
| `--result-ink-muted` | `#3A5F80` | 결과 보조 문구 |

`--result-fill`은 같은 화면에서 핵심 결과를 강조하는 용도로만 쓴다. 색칠된 카드가 여러 개 생기지 않게 한다.

---

## 5. 타이포그래피

### 폰트

- 한글·UI: Pretendard Variable
- 숫자: Geist Mono

### 위계

| 역할 | 크기 | 웨이트 |
|---|---:|---:|
| Home hero | 46px | 300 |
| Display | 32px | 300 |
| Heading | 24px | 350 |
| Subhead | 19px | 350 |
| Body | 16px | 400 |
| Caption | 13px | 450 |
| Main result | 60px | 400 |
| Result row | 20px | 400 |

모바일에서는 Hero 34px, Main result 48px로 줄인다.

계산 결과 숫자는 홈 헤드라인보다 시각적으로 강해야 한다. 숫자는 항상 tabular nums를 사용한다.

---

## 6. 레이아웃

- 전체 콘텐츠 최대 폭: 1120px
- 일반 본문 최대 폭: 640px
- 데스크톱 섹션 간격: 96px
- 모바일 섹션 간격: 56px
- 데스크톱 홈 2열 gap: 64px
- 카드 내부: 32px
- 모바일 카드 내부: 24px
- 모바일 좌우 여백: 16px

### 반응형

- 1024px 미만: 홈 2열을 1열로 전환
- 768px 미만: 입력·결과·근거 행을 모바일 구조로 전환
- 375px에서 가로 스크롤 0

---

## 7. 상단 탐색

데스크톱 기본 메뉴:

- 빠른 계산
- 정밀 분석
- 가이드
- 방법론

모바일에서는 우선순위가 낮은 항목을 숨겨도 된다. 모바일에서 항상 남아야 하는 것은 브랜드와 빠른 계산 또는 정밀 분석 진입점이다.

상단바는 1120px 폭을 사용하고 얇은 하단 헤어라인으로 제품 셸을 만든다.

---

## 8. 계산기

### 빠른 계산

첫 입력은 최소화한다.

- 건물 유형
- 지붕 면적
- 면적 단위

결과:

- 패널 배치 가능 면적
- 예상 패널 수
- 패널 한 장 출력
- 설치 가능 용량

설치 가능 용량은 `--result-fill`을 사용한 가장 강한 결과 면으로 보여준다.

### 추가 경제성 계산

발전시간, 손실률, 설치비, 자가소비율, SMP, REC 등 근거 없는 값을 자동으로 추정하지 않는다. 사용자가 직접 확인한 값을 넣는 구조를 유지한다.

### 다음 단계

기본 계산 이후에는 `/pro`로 자연스럽게 이어질 수 있어야 한다. 단, “전문가 상담”, “견적 받기” 같은 판매성 언어로 바꾸지 않는다.

---

## 9. 정밀 분석 `/pro`

전문가 화면은 일반 계산기의 복잡한 버전이 아니라 별도의 작업 공간처럼 보여야 한다.

- 좌측 또는 상단: 입력·프로젝트 조건
- 중심: 지도 또는 분석 대상
- 결과: 발전량·조건·출처
- 다운로드가 있다면 결과와 가까운 위치
- PVGIS 사용 여부와 데이터 버전 명확히 표시

데이터를 얻지 못한 상태를 0으로 표시하지 않는다. 로딩, 오류, 데이터 없음은 서로 구분한다.

---

## 10. 가이드와 신뢰 페이지

긴 글은 640px를 넘기지 않는다.

가이드의 목적은 SEO 문장을 늘리는 것이 아니라 계산 결과를 해석하게 만드는 것이다.

좋은 가이드 예:

- 지붕 면적과 실제 패널 배치 면적이 다른 이유
- 경사와 방위가 발전량에 미치는 영향
- 자가소비와 발전사업의 수익 구조 차이
- SMP와 REC를 어디서 확인하는가
- 현장 검토가 반드시 필요한 항목

---

## 11. 컴포넌트 규칙

### 입력

- 높이 48px 이상
- 항상 보이는 label
- 0.5px hairline
- radius 12px
- 오류는 필드 바로 아래
- 검증은 타이핑 중 남발하지 않는다

### 카드

- surface 배경
- hairline
- radius 16px
- box-shadow 금지

### 버튼

- 주 동작: ink 배경 + surface 글자
- 보조 동작: 투명 배경 + ink 테두리
- 높이 48px 이상
- pill 형태

### 링크

색으로 링크를 구분하지 않는다. 밑줄, 화살표, 위치와 문맥으로 어포던스를 만든다.

---

## 12. 모션

모션은 상태 변화를 설명할 때만 쓴다.

허용:

- 결과 등장 150~200ms opacity + 8px 이하 이동
- hover 시 8px 이하 이동
- 색·밑줄 전환

금지:

- 패럴랙스
- 자동 재생 배경 영상
- 카운트업 숫자
- 무한 반복 장식

`prefers-reduced-motion`을 항상 지원한다.

---

## 13. 카피

문장은 짧고 구체적으로 쓴다.

좋음:

- `지붕 면적만 넣으면 됩니다.`
- `현재 시장 단가를 임의로 넣지 않습니다.`
- `지붕 상태와 음영은 현장에서 확인해야 합니다.`

피함:

- `최적의 친환경 에너지 솔루션을 만나보세요.`
- `지금 바로 무료 상담을 받아보세요.`
- `AI가 완벽한 태양광 설계를 제공합니다.`

과장, 감탄사, 두루뭉술한 친환경 문구를 사용하지 않는다.

---

## 14. 접근성

- WCAG 2.1 AA 이상
- 터치 타깃 48px 이상
- 키보드 포커스 항상 표시
- `word-break: keep-all`
- 폼 label 필수
- 결과 변화에 `aria-live` 사용
- 색만으로 상태를 전달하지 않는다
- 375 / 768 / 1024 / 1440 확인

---

## 15. 완료 조건

UI 변경은 다음을 만족해야 완료다.

1. 첫 화면에서 계산을 시작할 수 있다.
2. 홈이 단순 계산기 하나가 아니라 빠른 계산→정밀 분석→가이드→방법론의 제품 흐름으로 보인다.
3. 입력 전에도 사이트가 무엇을 해결하는지 5초 안에 이해된다.
4. 계산 후 핵심 결과가 페이지에서 가장 강한 시각 요소다.
5. 추정값과 실제 외부 데이터가 섞이지 않는다.
6. 근거와 갱신 시점을 찾기 쉽다.
7. 영업 연락·견적 수집이 없다.
8. 가짜 시공 이미지가 없다.
9. 375px에서 가로 스크롤이 없다.
10. lint, typecheck, test, build가 통과한다.
11. Preview에서 데스크톱·모바일 실제 흐름을 확인한다.

---

## 16. 리뉴얼 방향 한 줄 요약

**계산기 한 장의 미니멀리즘은 유지하되, 그 계산기를 SolPlanit 전체 판단 흐름의 입구로 확장한다.**
