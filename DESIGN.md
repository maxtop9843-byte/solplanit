# DESIGN.md — SolPlanit Tool Site

> SolPlanit은 태양광 플랫폼이 아니라, 태양광을 검토하는 사람이 검색해서 들어와 바로 계산하고 판단할 수 있는 저관리형 전문 계산 도구 사이트다.

이 문서는 SolPlanit의 제품·UI 방향에 대한 최우선 기준이다.

---

## 0. 제품 전제

SolPlanit은 시공사, 견적 중개 서비스, 커뮤니티, 전문가 매칭 플랫폼이 아니다.

핵심은 단순하다.

**검색 → 무료 계산 → 결과 이해 → 관련 계산기로 이동**

사이트는 태양광 관련 계산과 판단 도구를 꾸준히 축적하는 SEO 자산형 서비스로 운영한다.

### 운영 원칙

1. 계정이 없어도 모든 핵심 계산기를 사용할 수 있어야 한다.
2. 전화번호·이메일을 수집하는 영업 CTA를 넣지 않는다.
3. 견적 연결, 전문가 매칭, 커뮤니티 운영을 제품 핵심으로 만들지 않는다.
4. 실시간 운영이 필요한 기능보다 계산 공식과 공개 데이터 기반 도구를 우선한다.
5. 계산 근거가 없는 단가·보조금·발전량을 임의로 자동 입력하지 않는다.
6. 숫자에는 출처, 갱신 시점, 가정 또는 한계가 따라야 한다.
7. 하나의 거대한 기능보다 검색 의도가 분명한 독립 계산기를 여러 개 만든다.

**북극성:** 한국에서 태양광 관련 계산이 필요할 때 가장 먼저 떠오르는 도구 사이트가 된다.

---

## 1. 하지 않는 것

현재 단계에서 아래 기능은 만들지 않는다.

- 시공사·전문가 매칭
- 견적 요청·리드 판매
- 커뮤니티
- 회원 전용 기능
- 프로젝트 관리 SaaS
- CRM
- 시공 사례 업로드 플랫폼
- 실시간 상담
- 운영자가 지속적으로 중개해야 하는 기능

트래픽이 충분히 생기기 전까지 플랫폼화하지 않는다.

---

## 2. 사이트 구조

### 홈 `/`

- SolPlanit의 대표 빠른 계산기
- 주요 태양광 계산기 진입점
- 계산 근거와 데이터 출처

### 계산기 `/calculators/*`

각 검색 의도별 독립 페이지를 만든다.

우선순위 예시:

1. 태양광 설치 가능 용량 계산기
2. 태양광 예상 발전량 계산기
3. 태양광 전기요금 절감 계산기
4. 태양광 수익 계산기 (SMP + REC)
5. 태양광 투자 회수기간 계산기
6. 패널 수량·필요 면적 계산기
7. 경사·방향에 따른 발전량 비교 계산기
8. PVGIS 정밀 발전량 계산기
9. 인버터 용량·DC/AC 비율 계산기
10. 월별 발전량 비교 계산기

### 정밀 계산 `/pro`

기존 `/pro`는 전문가 플랫폼이 아니다.

**PVGIS 기반 정밀 발전량 계산기**로 취급한다.

- 위치
- 경사
- 방위
- 시스템 용량
- 손실률
- PVGIS 데이터
- 월별/연간 발전량
- 출처와 데이터 버전

향후 더 적절한 URL이 필요하면 `/calculators/pvgis`로 이동할 수 있다.

### 가이드 `/guides/*`

계산기를 설명하고 검색 유입을 보조하는 짧고 실용적인 콘텐츠만 만든다.

### 신뢰 `/trust/*`

- 계산 공식
- 가정
- 데이터 출처
- 갱신일
- 현장 검토가 필요한 한계

---

## 3. 홈 화면

현재 Renewal V2 디자인을 유지한다.

- 1120px 제품 셸
- 데스크톱 2열
- 오른쪽 실제 계산기
- 차가운 오프화이트 + 검정 잉크 + 옅은 블루 결과 영역
- 결과 숫자가 가장 강한 시각 요소

단, 제품 메시지는 플랫폼보다 **도구 모음**에 맞춘다.

권장 메시지:

- Eyebrow: `SOLAR CALCULATORS / KOREA`
- H1: `태양광 설치, 감이 아니라 숫자로 결정하세요.`
- Trust line: `계정 없음 · 영업 연락 없음 · 계산 근거 공개`

홈에서 사용자가 이해해야 할 것은 복잡한 서비스 흐름이 아니라 다음 두 가지다.

1. 지금 바로 계산할 수 있다.
2. 더 필요한 계산은 다른 도구로 이어갈 수 있다.

---

## 4. 시각 언어

목표는 “친환경 홈페이지”가 아니라 “정밀한 기술 계산 도구”다.

### 사용한다

- 차가운 오프화이트 캔버스
- 거의 검정에 가까운 잉크
- 얇은 헤어라인
- 넓은 여백
- 숫자 전용 모노스페이스
- 결과 하나에만 쓰는 옅은 패널 블루
- 실제 폼, 표, 계산 결과, 데이터 흐름 자체를 시각 요소로 사용

### 사용하지 않는다

- 초록·노랑 메인 컬러
- 그라디언트 배경
- 카드 그림자
- 장식용 일러스트
- 스톡 태양광 사진
- AI 생성 패널·주택 이미지
- 이모지 기능 아이콘
- 과한 스크롤 애니메이션
- 숫자 카운트업

---

## 5. 색

| 토큰 | 값 | 역할 |
|---|---|---|
| `--canvas` | `#F2F2F4` | 전체 배경 |
| `--surface` | `#FDFDFD` | 폼과 카드 |
| `--ink` | `#0F1012` | 본문·주 동작 |
| `--ink-muted` | `#5E5E5E` | 보조 정보 |
| `--hairline` | `#D8D8DC` | 경계 |
| `--result-fill` | `#E4EDF7` | 핵심 계산 결과 면 |
| `--result-ink` | `#123A5E` | 결과 숫자 |
| `--result-ink-muted` | `#3A5F80` | 결과 보조 문구 |

---

## 6. 타이포그래피

- 한글·UI: Pretendard Variable
- 숫자: Geist Mono
- Home hero: 46px / 300
- Display: 32px / 300
- Heading: 24px / 350
- Subhead: 19px / 350
- Body: 16px / 400
- Caption: 13px / 450
- Main result: 60px / 400
- 모바일 Hero: 34px
- 모바일 Main result: 48px

계산 결과 숫자는 홈 헤드라인보다 강해야 한다.

---

## 7. 계산기 페이지 공통 구조

모든 계산기는 가능한 한 같은 구조를 사용한다.

1. 검색 의도가 분명한 H1
2. 1~5개의 핵심 입력
3. 즉시 계산 결과
4. 결과 해석
5. 사용한 공식·가정·출처
6. 관련 계산기 2~4개
7. 필요한 경우 짧은 가이드

### 입력

- 높이 48px 이상
- 항상 보이는 label
- 0.5px hairline
- radius 12px
- 오류는 필드 바로 아래

### 결과

- 가장 중요한 결과 하나만 `--result-fill` 사용
- 보조 결과는 행 또는 표
- 데이터 없음과 0을 구분
- 단위를 숫자와 명확히 연결

### 관련 도구

계산 완료 뒤 자연스럽게 다음 계산기로 이동하게 한다.

예:

설치 가능 용량 → 발전량 → 절감/수익 → 회수기간

판매성 CTA는 사용하지 않는다.

---

## 8. 데이터와 정확성

- 외부 데이터는 가능한 한 공식·공개 출처를 우선한다.
- PVGIS 사용 시 JRC와 데이터 버전을 표시한다.
- SMP·REC·전기요금처럼 시점에 따라 바뀌는 값은 자동 고정하지 않는다.
- 최신값을 자동으로 안정적으로 가져올 수 없는 경우 사용자가 입력하게 한다.
- 보조금은 지역·시점·사업별 차이가 크므로 근거 없이 예상액을 보여주지 않는다.
- 현장 음영, 구조 안전성, 실제 시공 가능 여부는 계산 결과와 구분한다.

---

## 9. SEO 원칙

SolPlanit의 성장 엔진은 플랫폼 네트워크 효과가 아니라 **검색 의도별 계산기 페이지 축적**이다.

- 계산기 하나 = 검색 의도 하나를 기본으로 한다.
- 비슷한 키워드를 억지로 한 페이지에 몰아넣지 않는다.
- 제목은 사용자가 실제 검색하는 표현을 우선한다.
- 계산 결과를 가리지 않는다.
- SEO 문장 때문에 계산기 사용성이 떨어지지 않게 한다.
- 관련 계산기 내부 링크로 세션 깊이를 높인다.
- 구조화 데이터, sitemap, canonical, locale 처리를 유지한다.

광고를 붙이더라도 입력과 결과 사이에는 넣지 않는다.

---

## 10. 수익화

초기 목표는 트래픽 검증이다.

우선순위:

1. 검색 유입
2. 계산기 사용량
3. 관련 계산기로 이동하는 비율
4. 재방문
5. 이후 광고 수익화

트래픽이 충분하지 않은 상태에서 유료 SaaS, 리드 판매, 중개 모델을 추가하지 않는다.

---

## 11. 접근성·반응형

- WCAG 2.1 AA 이상
- 터치 타깃 48px 이상
- 키보드 포커스 표시
- `word-break: keep-all`
- 폼 label 필수
- 결과 변화에 `aria-live`
- 색만으로 상태 전달 금지
- 375 / 768 / 1024 / 1440 확인
- 375px 가로 스크롤 0

---

## 12. 완료 조건

새 계산기 또는 UI 변경은 다음을 만족해야 한다.

1. 계산을 첫 화면에서 바로 시작할 수 있다.
2. 입력값이 실제 계산에 사용된다.
3. 핵심 결과가 가장 강하게 보인다.
4. 임의의 시장값·보조금·발전량을 꾸며내지 않는다.
5. 공식·가정·출처를 쉽게 찾을 수 있다.
6. 계정이나 영업 연락 없이 끝까지 사용할 수 있다.
7. 관련 계산기로 자연스럽게 이동할 수 있다.
8. 모바일 375px에서 깨지지 않는다.
9. lint, typecheck, test, build가 통과한다.

---

## 13. 한 줄 요약

**SolPlanit은 거대한 태양광 플랫폼을 만들지 않는다. 검색 수요가 있는 태양광 계산기를 하나씩 쌓아 장기적으로 트래픽이 축적되는 전문 도구 사이트를 만든다.**
