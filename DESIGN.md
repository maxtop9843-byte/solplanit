# DESIGN.md — SolPlanit Search-first V3

> SolPlanit은 태양광 설치를 검토하는 사람이 업체에 연락하기 전에 비용, 보조금, 발전량, 전기요금 절감 효과를 스스로 확인하는 태양광 계산 도구 사이트다.

이 문서는 제품 구조와 UI/UX의 최우선 기준이다.

---

## 0. 제품 한 줄 정의

**태양광 설치 전에 궁금한 숫자를 직접 확인하는 곳.**

성장 구조는 단순하게 유지한다.

**검색 → 해당 계산기 → 결과 이해 → 관련 계산기 → 재방문**

SolPlanit은 시공사, 견적 중개 서비스, 커뮤니티, 전문가 매칭 플랫폼이 아니다.

---

## 1. 사용자가 실제로 묻는 질문부터 시작한다

일반 사용자는 다음을 알고 싶어 한다.

1. 설치하면 얼마가 드나요?
2. 보조금을 받을 수 있나요?
3. 한 달 전기요금이 얼마나 줄어드나요?
4. 우리 지역에서는 얼마나 발전하나요?
5. 몇 년이면 설치비를 회수하나요?
6. 우리 지붕에는 몇 kW까지 설치할 수 있나요?
7. 3kW를 설치하면 비용과 발전량은 어느 정도인가요?
8. 설치 전에 꼭 확인할 단점이나 조건은 무엇인가요?

일반 사용자 화면에서 다음 질문을 먼저 하지 않는다.

- 평균 일 발전시간
- 시스템 손실률
- 자가소비율
- 자가소비 전력 가치
- SMP
- REC
- REC 가중치
- 위도·경도
- 모듈 기술

이 값들은 공식 데이터로 계산하거나, 고급 설정 또는 발전사업자 전용 계산기로 분리한다.

---

## 2. 입력 UX 원칙

### 사용자가 알고 있는 값부터 묻는다

기본 입력 후보는 다음 범위를 벗어나지 않는다.

- 설치 지역 또는 주소
- 건물 종류
- 월 전기 사용량(kWh) 또는 월 전기요금
- 지붕 면적, 알고 있을 때만
- 이미 정한 설치 용량, 알고 있을 때만

### 모르는 값은 모른다고 답할 수 있어야 한다

지붕 면적처럼 일반 사용자가 모를 수 있는 항목은 `잘 모르겠어요`를 허용한다. 모르는 값을 억지로 입력하게 하지 않는다.

### 전문 입력은 숨긴다

경사, 방위, 손실률 같은 값이 필요한 계산기는 기본값의 근거가 명확한 경우에만 기본값을 제공한다. 그 외에는 `상세 조건` 안으로 넣거나 정밀 계산기로 분리한다.

### 입력하지 않은 값은 결과를 만드는 척하지 않는다

공식 데이터가 없거나 신뢰할 수 없는 값은 계산하지 않는다. `정보 없음`과 `0원`, `0kWh`를 구분한다.

---

## 3. 정보 구조

### 홈 `/`

홈은 계산기 하나를 과하게 확장하는 곳이 아니라 **검색 수요가 큰 계산기로 들어가는 허브**다.

첫 화면에는 현재 완성된 대표 계산기를 바로 사용할 수 있게 둔다. 그 아래에는 실제로 사용할 수 있는 계산기만 노출한다. 준비 중인 도구를 링크처럼 보여주지 않는다.

### 핵심 계산기 `/calculators/*`

우선순위는 다음과 같다.

1. `solar-3kw-cost` — 3kW 태양광 설치비용 계산기
2. `subsidy` — 2026 태양광 보조금 조회
3. `bill-savings` — 태양광 전기요금 절감 계산기
4. `generation` — 지역별 태양광 예상 발전량 계산기
5. `payback` — 태양광 투자 회수기간 계산기
6. `roof-capacity` — 지붕 면적 기준 설치 가능 용량 계산기
7. `panel-count` — 패널 수량·필요 면적 계산기
8. `orientation` — 경사·방향별 발전량 비교

### 정밀 발전량 `/pro`

`/pro`는 전문가 플랫폼이 아니다. **PVGIS 기반 정밀 발전량 계산기**다.

지도에서 위치를 고르고, 설치 용량·경사·방위 등을 조정해 월별·연간 발전량을 확인한다.

### 발전사업자 계산기

SMP·REC는 주택용 절감 계산과 섞지 않는다. 별도의 발전사업자 계산기로 분리한다.

---

## 4. 메인 카피 원칙

### 기본 톤

- 짧고 구체적인 한국어
- 실제 사용자가 말하는 단어
- 전문용어가 필요하면 바로 설명
- 과장하지 않음
- 영업 문구 없음

### 좋은 문장

- `우리 건물에 태양광, 얼마나 설치할 수 있을까요?`
- `지붕 면적을 대략 넣으면 설치 가능한 용량과 패널 수를 계산합니다.`
- `정확한 면적을 몰라도 괜찮아요.`
- `이 결과는 지붕 형태와 음영에 따라 달라질 수 있습니다.`
- `월 전기 사용량은 고지서에서 확인할 수 있어요.`
- `이 지역의 2026년 지원 내용을 확인했습니다.`

### 피할 문장

- `감이 아니라 숫자로 결정하세요.`처럼 광고 슬로건에 가까운 표현을 반복하지 않는다.
- `최적의`, `완벽한`, `혁신적인`, `스마트한` 같은 추상적인 수식어를 쓰지 않는다.
- `자가소비 전력 가치`, `평균 일 발전시간` 같은 업계 표현을 일반 사용자 첫 화면에 그대로 쓰지 않는다.
- `견적 받기`, `상담 신청` 같은 판매 CTA를 넣지 않는다.

### 한국어 검수 체크

UI 문구를 바꿀 때 다음을 확인한다.

1. 번역체 어순이 없는가?
2. 조사가 어색하지 않은가?
3. 명사만 연달아 붙인 행정 문구가 아닌가?
4. 같은 뜻을 두 번 말하지 않는가?
5. 초등 고학년이 읽어도 뜻이 바로 전달되는가?
6. 버튼 문구가 실제 동작을 설명하는가?

---

## 5. 결과 UX

결과는 사용자의 질문에 바로 답해야 한다.

예시:

- `예상 설치 가능 용량 5.4kW`
- `연간 예상 발전량 7,120kWh`
- `예상 연간 절감액 94만~112만원`
- `확인된 보조금 265만원`
- `예상 실부담액 189만원`
- `단순 회수기간 약 7.4년`

범위로 보여주는 것이 더 정직한 값은 억지로 하나의 숫자로 만들지 않는다.

### 결과 순서

1. 사용자가 가장 궁금해한 핵심 결과
2. 핵심 결과에 영향을 준 입력
3. 보조 결과
4. `이 숫자는 어떻게 계산했나요?`
5. 관련 계산기

`--result-fill`은 한 화면에서 가장 중요한 결과 하나에만 사용한다.

---

## 6. 데이터와 신뢰

### 공식 데이터 우선순위

1. 한국에너지공단 신재생에너지센터
2. 정부·지자체 공고
3. 한국전력 공식 요금 자료
4. 전력거래소
5. JRC PVGIS
6. 제조사 공식 사양

### 보조금

- 연도와 지역을 함께 저장한다.
- 공식 공고를 확인한 지역만 구체적인 지원액을 표시한다.
- 다른 지역의 금액을 추정해 적용하지 않는다.
- `2026년`, `확인일`, `출처`를 결과 가까이에 표시한다.

### 설치비

- 공식 기준단가와 실제 시장 견적을 구분한다.
- 공식 기준단가가 있으면 `공식 사업 기준`이라고 표시한다.
- 임의의 전국 평균 설치비를 기본값으로 고정하지 않는다.

### 전기요금 절감

- 고정 `원/kWh` 하나로 계산하지 않는다.
- 한국전력 요금 체계를 반영할 수 있을 때만 자동 계산한다.
- 그렇지 않으면 사용량 변화까지만 보여주고 금액 계산은 보류한다.

### 발전량

- 위치 기반 발전량은 PVGIS 같은 실제 데이터로 계산한다.
- 지역 대표 좌표를 쓸 경우 `지역 중심 기준의 간단한 예상치`임을 표시한다.
- 정확한 주소 또는 지도 좌표를 사용한 결과와 구분한다.

---

## 7. 검색 의도와 SEO

한 계산기 페이지는 하나의 핵심 질문에 집중한다.

제목 예:

- `3kW 태양광 설치비용 계산기 | 2026 보조금 반영`
- `2026 태양광 보조금 조회 | 지역별 지원 확인`
- `태양광 전기요금 절감 계산기`
- `태양광 발전량 계산기 | 지역별 예상 발전량`
- `태양광 회수기간 계산기`

SEO 문장을 늘리기 위해 입력과 결과를 아래로 밀지 않는다. 계산기는 첫 화면에서 시작할 수 있어야 한다.

Search Console 데이터가 쌓이면 실제 검색어·노출·CTR을 기준으로 우선순위를 다시 정한다.

---

## 8. 시각 언어

현재 승인된 Renewal V2의 시각 언어를 유지한다.

- 차가운 오프화이트 캔버스
- 거의 검정에 가까운 잉크
- 얇은 헤어라인
- 넓은 여백
- Pretendard Variable
- 숫자에 Geist Mono
- 핵심 결과에만 옅은 블루
- 그림자 없음
- 초록·노랑 메인 컬러 없음
- 스톡 태양광 사진 없음
- AI 생성 패널 이미지 없음

기본 토큰:

| 토큰 | 값 |
|---|---|
| `--canvas` | `#F2F2F4` |
| `--surface` | `#FDFDFD` |
| `--ink` | `#0F1012` |
| `--ink-muted` | `#5E5E5E` |
| `--hairline` | `#D8D8DC` |
| `--result-fill` | `#E4EDF7` |
| `--result-ink` | `#123A5E` |
| `--result-ink-muted` | `#3A5F80` |

---

## 9. 계산기 공통 레이아웃

1. 검색 의도가 분명한 H1
2. 한 문장 설명
3. 핵심 입력 1~4개
4. 계산 버튼 또는 즉시 결과
5. 핵심 결과
6. 보조 결과
7. 계산 기준과 한계
8. 관련 계산기 2~4개

입력 높이는 48px 이상, 모바일 375px에서 가로 스크롤이 없어야 한다.

---

## 10. 하지 않는 것

- 커뮤니티
- 시공사·전문가 매칭
- 견적 요청 폼
- 전화번호·이메일 리드 수집
- 회원 전용 계산기
- CRM
- 프로젝트 관리 SaaS
- 실시간 상담
- 근거 없는 AI 추천

트래픽과 사용자 행동 데이터가 충분히 쌓이기 전에는 다시 플랫폼화하지 않는다.

---

## 11. 완료 조건

새 계산기나 UI 변경은 다음을 모두 만족해야 한다.

1. 사용자가 페이지 제목만 보고 무엇을 계산하는지 안다.
2. 일반 사용자에게 전문가용 변수를 먼저 묻지 않는다.
3. 입력한 값은 실제 계산에 사용된다.
4. 핵심 결과가 가장 먼저 이해된다.
5. 임의의 시장가격·보조금·발전량을 꾸며내지 않는다.
6. 공식·가정·출처를 결과 가까이에서 확인할 수 있다.
7. 한국어 문장이 자연스럽다.
8. 판매성 CTA 없이 끝까지 사용할 수 있다.
9. 모바일 375px에서 깨지지 않는다.
10. lint, typecheck, test, build가 통과한다.

---

## 12. 북극성

**SolPlanit은 태양광을 아는 사람이 쓰는 계산기가 아니라, 태양광을 알아보는 사람이 이해할 수 있는 계산기를 만든다.**
